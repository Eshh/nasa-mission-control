describe("get launches api", () => {
  test("It shouldrespond with 200", () => {
    const res = 200;
    expect(res).toBe(200);
  });
});

describe("add new launch api", () => {
  test("It should catch missing properties error", () => {});
  test("It should catch invalid date error", () => {});
});
