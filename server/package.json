{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --config ./test/jest-e2e.json --detectOpenHandles --watchAll --no-cache",
    "test-watch": "jest --watch",
    "watch": "nodemon src/server.js",
    "start": "node src/server.js",
    "cluster": "pm2 start src/server.js -i max --watch -l logs.txt --no-daemon"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "cors": "^2.8.5",
    "csv-parse": "^5.4.0",
    "express": "^4.18.2",
    "mongoose": "^7.4.3",
    "morgan": "^1.10.0",
    "save-dev": "^0.0.1-security",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "jest": "^29.6.2"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
